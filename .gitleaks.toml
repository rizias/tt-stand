[extend]
useDefault = true

[allowlist]
description = "Примеры разбора токена в документации: рядом с токеном напечатана его расшифровка, подмена значения сделала бы пример неверным"
paths = ['''docs/output\.md''']
